<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Courses" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/black</item>

    </style>

    <style name="Theme.Courses" parent="Base.Theme.Courses" />


        <!-- Базовый стиль для BottomNavigationView -->
        <style name="BottomNavigationTheme" parent="Widget.MaterialComponents.BottomNavigationView">
            <item name="android:background">@color/dark_grey</item> <!-- Черный фон -->
            <item name="itemIconTint">@drawable/bottom_nav_icon_color</item> <!-- Цвет иконок -->
            <item name="itemTextColor">@drawable/bottom_nav_text_color</item> <!-- Цвет текста -->
        </style>

</resources>